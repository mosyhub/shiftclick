const express = require('express');
const router = express.Router();

// Review routes - placeholder
router.get('/', (req, res) => res.json({ message: 'Reviews API ready' }));

module.exports = router;
